package net.http
{
    
    /**
     * The contract of an HTTP Configuration.
     * 
     * <p>
     * This interface is empty on purpose,
     * we want by default our core classes to be without configurations.
     * 
     * A configuration starts to be needed for advanced applications,
     * see <code>WebConfig</code> and <code>WebGateway</code>.
     * </p>
     */
    public interface Configuration
    {
        
    }
}