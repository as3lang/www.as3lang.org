package
{
    /**
     * The global object allow to share variables
     * between the scope of the currently executing code
     * and the scope of evaluated code. 
     */
    public var _global:Object = {};
}